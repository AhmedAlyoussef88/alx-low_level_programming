#include "main.h"
/**
 * _isdigit - checks if input is digit
 * @c: input integer of this function
 * Return: 1 if digit else 0
 */
int _isupper(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
