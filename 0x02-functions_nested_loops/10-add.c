#include "main.h"
/**
 * add - adds two integers and returns the result
 * @x : Input parameter
 * @y : Input Parameter
 * Return: result of the two inputs sum
 */
int add(int x, int y)
{
	return (x + y);
}
